//
//  File.swift
//  
//
//  Created by Ronan Furuta on 8/16/23.
//

import Foundation
import Vapor
import ArrivalGTFS
/*
struct HashesController: RouteCollection {
    func boot(routes: RoutesBuilder) throws {
        let route = routes.grouped("v2").grouped("db")
        route.get("hashes", use: hashes)
        
      
    }
    func hashes(req: Request) async throws -> HashesResponse {
        return .init()
    }
}
struct HashesResponse: Content {
    //  var stationsDB: String = agtfs.db.signature()
    var trips: String = agtfs.db.trips.signature()
    var stopTimes: String = agtfs.db.stopTimes.signature()
    var routesDB: String = agtfs.db.routes.signature()
}
*/
